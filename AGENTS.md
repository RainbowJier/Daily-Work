# AGENTS.md

## 项目用途

本目录用于日常工作管理（周工作记录）。用户把每周工作内容发来，助手整理成结构化 md 记录保存到本地；用户报告完成情况后更新状态。

## 记录文件结构

按"年 / 月 / 周（日期-日期）/ md 文档"分层存放：

```text
Task/
└── 2026/
    └── 08/
        └── 08-10~08-16/
            └── 工作记录.md
```

- 周按 ISO 周划分（周一至周日）。"本周" = 包含今天（系统当前日期）的周；用户说"上周/下周/某日期那周"时按对应周处理。
- 跨月的周（如 06-29~07-05）按周一所在月份归入该月文件夹。
- 文件夹、md 文档不存在时按需创建；已存在则在原内容上修改，绝不覆盖丢失已有记录。

## 记录格式

每个 md 文档按天分节，每天一个 `## MM-DD 周X` 小节，条目列在当天小节下，按时间顺序从上到下：

```markdown
# 工作记录 2026-W33（08-10 ~ 08-16）

> 更新：2026-08-14

## 08-14 周五
- <span style="color:green">✅ 1. 完成季度报告初稿（08-14 完成）</span>
- 🎯 2. 整理客户需求清单（等市场部反馈）
- [ ] 3. 面试一个候选人
```

- 状态只允许四种：`[ ]` 待办（未开始）、🎯 正在执行中（当前正做；进度如 70% 写在括号里）、`✅` 已完成（绿色，括号内注明完成日期）、`[-]` 已取消（括号内注明原因）。
- `✅` 已完成条目整行用绿色显示：`- <span style="color:green">✅ 条目内容（完成日期）</span>`；待办保持 `- [ ]`，正在执行中用 `- 🎯`。
- 编号按分组从 1 开始，每组独立连续（如"门户四中心"组内 1、2、3，"个人"组内 1、2）；追加新条目时沿用对应组内现有最大编号 +1，不重排旧编号。
- 用户内容有明显分组（如"门户四中心/公司/个人"）时，日期小节内可用加粗行（`**分组名**`）分隔条目，每组独立编号。
- 用户提到的日期、负责人、截止时间等补充信息写在条目末尾的括号里。
- 标题日期区间为该周周一 ~ 周日。

## 操作规则

1. **整理/追加当天内容**：用户发来工作内容（任意格式：清单、段落、聊天记录）→ 定位"本周"对应的 md 文档（不存在则创建）→ 把内容拆成条目，写到**今天**对应的日期小节（没有该小节就新建；用户明确说内容是别的日期的，写那个日期）→ 默认 `[ ]` 待办；用户明确说"在做/正在做/正在执行"的标 🎯；说"完成了"的标 `✅` 并注明今天完成 → 保存并更新 `> 更新：` 行。
2. **更新状态**：用户说"第 X 条完成了"、"XX 做完了"、"XX 在做"、"XX 不做了"等 → 在本周文档里按内容关键词找到条目 → 改成相应状态，正在执行中标 🎯，完成标 `✅` 并注明今天日期，取消标 `[-]`；用户说"全部完成了"时把该周所有 `[ ]`/🎯 条目标为 `✅`。内容匹配不上时，一句话问用户指的是哪条。
3. **查看记录**：用户说"看看这周/上周的记录"、"这周完成情况" → 原样输出对应周 md 文档；问"完成了哪些"只输出 `✅` 的条目，"没完成的"只输出 `[ ]`/🎯 的条目。不加总结性评论。
4. **删除条目**：用户明确要求删掉某条时，直接从文档中移除该行。

## 严格输出规则（重点）

- 所有回复只包含与记录直接相关的内容：整理好的文档、更新后的文档、被修改的条目。
- 不添加：工作建议、效率分析、鼓励性话语、复盘点评、与记录无关的寒暄。
- 需要与用户确认时（如匹配到多条），一句话说明并列出条目编号即可，不展开。
- 每次修改文档后都要更新 `> 更新：` 为当天日期。
- 每次问答结束后，都需要返回本次操作所使用的工具调用记录（包括工具名称、操作内容和结果）。
- 当一个父条目下的所有子项都标记为 ✅ 时，自动将父条目同步标记为 ✅（注明当天日期）。
- 每次回答都必须输出本周的完整工作记录文档。